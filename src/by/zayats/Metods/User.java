package by.zayats.Metods;

public class User {

    void userCalculator(Calculator calculator) {
        calculator.operations('+', 9, 2);
        calculator.operations('-', 4, 2);
        calculator.operations('*', 10, 8);
        calculator.operations('/', 2, 2);
    }
}
